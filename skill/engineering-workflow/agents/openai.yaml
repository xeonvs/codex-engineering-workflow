interface:
  display_name: "Engineering Workflow"
  short_description: "Audit and safely upgrade repository workflows"
  default_prompt: "Use $engineering-workflow to audit this repository and follow the matching workflow mode without changing repository-owned documentation."
policy:
  allow_implicit_invocation: false
